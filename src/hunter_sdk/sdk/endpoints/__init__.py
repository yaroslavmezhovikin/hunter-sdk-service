"""Hunter.io API endpoints.

Each endpoint is a small callable object in its own module. Adding a new
endpoint means adding a new file here, never modifying the transport or
an existing endpoint.
"""
